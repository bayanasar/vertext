# Vertext for Neovim

The Neovim product will be a Lua plugin backed by the `vertext` CLI. Its first
mode is a read-only split preview with source-to-layout cursor mapping. A TUI
cannot rotate arbitrary glyphs, so accurate traditional Mongolian requires a
vertical-capable GUI frontend; the preview remains useful for CJK and stacked
Latin words.
